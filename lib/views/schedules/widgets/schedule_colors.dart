import 'package:flutter/material.dart';

class ScheduleColors {
  ScheduleColors._();

  static const primary = Color(0xFFE8356D);
  static const primaryLight = Color(0xFFFCE4EC);
  static const primaryDark = Color(0xFFC2185B);

  static const accent = Color(0xFF2196F3);
  static const accentLight = Color(0xFFE3F2FD);

  static const thumbTop = Color(0xFF2D1B6E);
  static const thumbBottom = Color(0xFF1A0A3D);

  static const surface = Color(0xFFFFFFFF);
  static const background = Color(0xFFF5F5F7);
  static const border = Color(0xFFE0E0E0);

  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF757575);

  static const Color primaryPink = Color(
    0xFFE6004C,
  ); // bright pink used in selected pill background
  static const Color primaryBlue = Color(
    0xFF00AEEF,
  ); // bright blue for the dot under selected pill
}
